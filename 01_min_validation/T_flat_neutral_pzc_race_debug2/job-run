#!/bin/bash
# Race copy on `debug`, parallel to shared job 20847214. Same fix applied
# (KPAR=4, NCORE=4, 6 layers unchanged). Whichever starts first wins; the
# other gets cancelled.
#SBATCH --job-name=Au_T_neutral_pzc_debug
#SBATCH -o myjob.o%j
#SBATCH -e myjob.e%j
#SBATCH --nodes=1
#SBATCH --ntasks=32
#SBATCH --mem=100G
#SBATCH --cpus-per-task=1
#SBATCH --time=02:00:00
#SBATCH --partition=debug
#SBATCH --account=CHE190065

module purge
module load intel/19.0.5.281 impi/2019.5.281
module load intel-mkl hdf5 python
module list

export OMP_NUM_THREADS=$SLURM_CPUS_PER_TASK
PW=/anvil/projects/x-che190065/rywang/CEP-HALF/bin
runvasp="mpirun -np $SLURM_NTASKS $PW/vasp_std  > log.out"

eval $runvasp
