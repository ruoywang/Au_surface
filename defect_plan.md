# Defect / Morphology Plan — Round 1 Candidate Parents (rev 2)

Status: **planning + geometry construction, no new DFT submitted.** Corrects four
concrete errors found in rev 1 (A/B step-label convention, strip atom-count formula,
island/pit base-cell sizing, kink periodicity definition) and adds the missing
geometry-check fields so future construction scripts are checked automatically
instead of hand-typed into a table.

## Open item, not blocking this plan

μ_e^SHE and the internal TARGETMU scale are still uncalibrated. The RHE→μ_e relation
itself is fixed (μ_e = μ_e^SHE − eU_RHE + k_B T ln10·pH; at 298.15 K, pH 7, 1.0 V vs
RHE → μ_e = μ_e^SHE − 0.586 eV), but μ_e^SHE for this code/pseudopotential/solvent
combination is not established. "已经核对" in an earlier instruction was an
inaccurate phrasing, not a pointer to an existing calibration. Every structure below
is planned independent of that number.

## Field schema — script-generated, never hand-filled

**Physical role** (relaxed_candidate / constrained_model / off_equilibrium_training)
is decided by the *actual* constraint and relaxation state, not by which family a
structure belongs to. Two extra fields travel with it:
- `relaxation_status`: not_run / relaxed
- `morphology_restraint`: none / <specific constraint, e.g. "top-layer registry fixed">

Fixing the bottom reference layers (a slab-boundary approximation used everywhere) is
NOT the same kind of constraint as forcibly fixing a defect's shape or registry — the
two must be recorded separately, not both collapsed into "constrained_model."

**Pipeline status**: planned / geometry_only / converged / qc_passed. An entry with no
generated coordinates yet is `planned`, not `geometry_only`.

**Per-structure geometry record** (script output only, atom counts in tables below are
provisional until confirmed by this record):
`N_base, N_added, N_removed, N_total, in_plane_cell_vectors, L_parallel, L_perpendicular,
terrace_width_upper, terrace_width_lower, defect_image_min_distance,
periodic_connectivity (finite / merged_strip / merged_network),
step_microfacet_per_edge {terrace_orientation, step_edge_direction, step_microfacet,
step_label, step_label_convention}, minimum_Au_Au_distance, reference_id`

Matched flat/undefected references (one per distinct in-plane cell + layer count +
constraint scheme) are auto-generated by script and do NOT count against the
20–30 parent budget.

## Correction 1 — A/B step-label convention was inverted

Correct convention: **A-type = {100} microfacet, B-type = {111} microfacet.**

| surface | step microfacet | label |
|---|---|---|
| Au(211) | {100} | A-type |
| Au(221) | {111} | B-type |
| Au(332) | {111} | B-type |
| Au(554) | {111} | B-type |

Au(211) [3(111)×(100)] and Au(221) [n(111)×(111)] differ in both microfacet type *and*
terrace width at once — comparing them can't cleanly isolate "step type" alone; this
limitation gets stated explicitly wherever the pair is used, not papered over by
adding more surfaces. Primary fields carried per structure are `terrace_orientation`,
`step_edge_direction`, `step_microfacet`, `step_label`, `step_label_convention` — the
A/B letter is a derived label, never the primary field, so a source using the opposite
convention can't silently flip a result.

## Correction 2 — strip atom counts, recomputed from the actual formula

4-layer base + half-coverage strip: N_base = 4·nx·ny, N_strip = 0.5·nx·ny,
N_total = 4.5·nx·ny.

| structure | nx×ny | N_base | N_strip | N_total | status |
|---|---|---|---|---|---|
| Step-8x4 | 8×4 | 128 | 16 | **144** | geometry_only, built + rendered |
| Step-16x4 | 16×4 | 256 | 32 | **288** | geometry_only, registry bug fixed, rendered (3-panel + whitebg), edge coordination re-checked (see below) |
| Step-24x4 | 24×4 | 384 | 48 | **432** | geometry_only, built; render pending (Batch 2, low-cost, can run alongside other Batch 2 work) |

Rev 1 had written 160/288/416 — it reused Step-16x4's actual 32-atom strip count for
all three sizes instead of rescaling with nx·ny. Fixed here; going forward atom counts
come from `scripts/geom_check.py` output, not a hand-filled table.

Still open: this is a **fixed-half-coverage** series (both terraces widen together as
nx grows), not a fixed-strip-width series (only the outer terrace widens) — the two
are different studies and this round commits to the half-coverage definition only.
Width is measured along m̂ = ẑ×t̂ (t̂ = step direction), not as half a raw
(non-orthogonal) lattice vector — done in `scripts/build_manifest.py`; Step-16x4 comes
out to 20.37 Å/side (vs. an earlier naive estimate of 23.5 Å, ~13% off).

**Edge classification, re-checked (`scripts/check_step_edges.py`)**: an earlier claim
that the two edges were "verified NOT equivalent (coordination 12 vs 11)" is
**retracted** — it compared foot atoms (base layer) using `get_distances(mic=True)`,
which returns only the single nearest periodic image per atom and can silently miss
additional images of the same atom in a small cell; the neighbor count itself was
never audited. Redone with explicit (atom, in-plane lattice shift) enumeration at
3.2/3.4/3.6 Å cutoffs (stable across all three): the actual upper-edge atoms
(edge1_top, edge2_top) both have **CN=7 with identical shell composition** (3 below +
4 in-layer + 0 above). This is suggestive of equivalence but does **not** confirm full
crystallographic equivalence (registry beyond the first shell not checked), and
equally does not establish an A/B pair. Status stays open — carried in
`manifest.json` as `edge_classification_status`, not asserted either way.

## Correction 3 — island/pit base cells were geometrically infeasible

A 4×4 (64-atom) base has 16 lattice sites per layer. 19 same-layer, same-registry,
non-overlapping atoms cannot fit in that periodic cell — not a boundary-effect
concern, a hard infeasibility. Even 7 atoms at 7/16 = 43.75% coverage is too dense to
assume isolation from its own periodic image by default.

Corrected screening starting points (starting points for geometry screening, not
converged final sizes — final size is set by real coordinates, connectivity, periodic
distance, and later response convergence):

| structure | starting cell | note |
|---|---|---|
| Island-7, Pit-7 | 6×6 | still needs edge–image distance check |
| Island-19, Pit-19 | 8×8 | elongated shapes may need larger/different aspect ratio |
| compact/elongated pairs | same cell, sized for the elongated shape | keeps shape and periodicity from being conflated |

Mandatory automated check for every island/pit: is it still finite under PBC, or has
it merged with its own image into an infinite strip/network (island) or trench (pit)?
If merged, it gets renamed, not kept as "isolated island/pit" — this bears directly on
whether the continuum ion model can actually resolve it as a separate feature.

## Correction 4 — kink periodicity was underspecified

Need both `L_parallel` (period along the step direction) and `L_perpendicular`
recorded, plus the periodic-equivalent nearest spacing s_min = min(s, L_parallel − s).
In a short along-edge period, increasing s from 0.4·L to 0.8·L actually *decreases*
s_min from 0.4L to 0.2L — a "long" spacing can be closer to the far mirror image than
the near one if this isn't checked.

Plan: define short/long spacings inside the *same*, sufficiently long along-edge
period; separately test coupling against an even longer period before trusting either
as "isolated." This round: plan (not build yet) extending the along-edge repeat from
today's 4 primitive rows (the "4" in "16×4") to 8. After construction, re-count actual
kinks produced — an atom-conserving rearrangement can create more than 2 corners, or
simultaneous edge vacancies + bulges; a kink pair is confirmed by inspection, not
assumed from "moved a set of atoms."

## Reference structures

Every modified-Au(111) structure (steps, islands, pits, composites) gets a matched
flat reference at the same in-plane cell, layer count, and constraint scheme,
auto-generated by script — not a separate parent slot. High-index vicinal slabs are
compared to Au(111) via consistent potential/numerics and area- or step-length
normalization, never by subtracting against a geometrically-invalid same-cell flat
"(111) co-parent."

## Slab thickness

4 layers stays the working baseline, explicitly **not** a validated production
thickness. A 4-layer vs. 6-layer (and, if needed, 8-layer) comparison on the flat
reference and one representative step is planned, not submitted, this round.
VASPsol++'s own Au(111) benchmark used a 6-layer slab — this doesn't prove 4 wrong or
6 universally sufficient, only that 4 isn't an unchecked-safe default. Separately: a
monolayer pit in a 4-layer base leaves only 3 atomic layers under the pit floor —
pit-floor/back-side coupling is worth checking specifically, likely more sensitive
than the flat 4-layer case.

## V7 / composite / reconstruction — confirmed, no rebuild

- **V7 → Pit-7**: confirmed. Original construction tag ("seven-vacancy cluster"),
  geometry, and image kept as-is. Different-size pit comparisons still need their own
  matched flat reference.
- **IslandPit-pair**: kept as a construction template only. One spacing tests the
  geometry/analysis pipeline, not non-additivity; later instantiate ≥2 spacings with
  matched standalone island/pit/flat references in the same cell.
- **Reconstruction placeholders (2 entries)**: kept as templates to concretize, agreed
  — but not yet counted as "covered reconstruction environment" in any dataset
  coverage statistic. When generated, each must report top-layer atom count, local
  displacement field, fcc/hcp registry assignment, domain-boundary description, and
  periodic closure method. Must not be built by splicing two incompatible lattices to
  fake a strained seam and calling it reconstruction.

## Build order — validate one construction method, then extend its series

**Batch 1 — representative structures** (prove counting/stacking/microfacet/periodic-
connectivity/diagrams are all correct):
reuse T / V1 / A1-fcc / Pit-7 (V7); verify Step-16x4 (corrected count, per-edge
geometry, true perpendicular width, L_parallel/L_perpendicular); build Au(211),
Au(221); build one properly-sized Island-7 (6×6 start).

**Batch 2 — scale & shape** (prove that varying width/size/shape actually varies the
claimed thing): corrected Step-8x4/24x4 series; Au(332)/Au(554); Island-19/Pit-19;
compact/elongated and corner-variant comparisons.

**Batch 3 — along-edge & composite environments** (prove a local defect is
distinguishable from its own periodic image and from other defects): kink series with
lengthened along-edge period; island-pit template at ≥2 spacings; concretized
reconstruction candidates.

All three batches are geometry construction only, no DFT submission implied. Entries
that turn out redundant, uneconomical, or unable to cleanly isolate their claimed
variable after geometry checking get merged or deferred — the 20–30 count is not a
completion requirement. Step-24x4 specifically (432 Au already at 4 layers) is a
convergence candidate, not something Batch 1/2 needs to finish.

## Diagram requirements

Each representative structure gets: central-cell top view, key-direction side view,
**and a periodic-mirror view** (neighboring cell copies shown) — mandatory for
islands/pits/kinks specifically, since a single isolated crop can visually disguise an
actually-merged periodic strip/network as a finite island. Diagrams mark: cell
boundary, upper/lower terrace, step direction, microfacet type, defect size, and the
actually-measured periodic distance (not a nominal one). Compact and elongated islands
use the same visual scale; different layers/defect atoms may be color-distinguished
but never rendered with occlusion hiding overlapping atoms.

## DFT budget triage (rev 3) — geometry library ≠ DFT queue

Every structure built above is a **geometry candidate**, not an automatic DFT submission.
Two things were being conflated in rev 2 and are now split:

1. **Structure family coverage** (flat / straight step / island / pit — the actual
   research question) vs. **size variants within a family** (Step-8x4 vs -16x4 vs -24x4;
   Island-7 vs -19; Au221 vs -332 vs -554). Adding a size variant is not the same as
   adding a physical environment that needs its own training coverage.
2. **What's geometrically built** vs. **what should be computed next**. A structure can
   be a legitimate, finished geometry and still not belong in the next DFT batch.

**Step series was carrying 4x redundant along-step repeats for no physical reason.**
The strip construction uses ny=4 along the step direction (`a2`) with no kink or
perturbation breaking translational symmetry there — an idealized straight step has
no periodicity requirement beyond the primitive translation (2.94 Å = one nearest-
neighbor spacing). Built and validated (`scripts/build_step_reduced.py`): Step-8x1/
16x1/24x1 at ny=1, and Step-8x2/16x2/24x2 at ny=2 as a safety margin. Validation: the
ny=1 cell tiled ×4 along `a2` reproduces the ny=4 build's coordinates atom-for-atom.
Terrace width (which depends only on `nx`) is unchanged — Step-24x1 keeps the full
30.6 Å/side terrace at 108 atoms instead of 432. This only holds for the idealized
straight step; a kink or any along-edge perturbation needs a longer period than this
and should NOT be built by naively shrinking ny.

**`dft_queue_status` field** (in `manifest.json`, separate from `role`/`pipeline_status`):
- `computed` — T/V1/A1-fcc, already has DFT results
- `queued` — small (≲110 atoms), candidate for the next DFT batch: Pit-7 (137 already
  built, kept as the minor point-defect supplement), Step-8x1/16x1/24x1, Au211/221/332/554,
  Island-7 (6×6, 151 atoms)
- `validation_candidate` — kept for a later size/isolation check, not bulk-computed now:
  Step-8x2/16x2/24x2, Island-7-8x8/Island-19-8x8/Pit-7-8x8/Pit-19-8x8
- `ML_application` — geometry/render reference and large-system ML testing only, not in
  the near-term DFT queue: Step-8x4/16x4/24x4 (superseded for DFT purposes by the x1
  reduced cells; kept because they're already built, rendered, and useful as an ML/
  large-system reference)

None of the `validation_candidate`/`ML_application` entries are deleted — they stay in
the geometry library. The high-index vicinal slabs (40–72 atoms each) are already small
enough to be strong first-batch DFT candidates; picking which 2 of the 4 answer the most
useful question is a separate scientific call, not made here.

k-points, NELECT, and net charge are NOT carried over unchanged when a cell shrinks —
each reduced cell needs its own k-mesh (denser along the direction that got shorter)
and its own electron count scaled to the same target potential, not the same absolute
charge. None of this has been set up yet; it's a reminder for whoever builds the actual
DFT inputs for the reduced cells, not something resolved by the geometry step above.

## Phase 2 — recorded, not built this round

Au(100) flat reference; one (100)-terrace stepped surface (exact Miller index TBD);
larger-scale composite morphologies. Not expanded further just to fill a quota.
